-- =============================================================================
-- 011 — จำนวนคงเหลือของประกาศ
-- null = ไม่จำกัด (ของสดที่มีเรื่อย ๆ เช่น ผักกำละ 20)
-- ตัวเลข = มีเท่านี้ · หมดแล้วระบบปิดประกาศเป็น "ขายแล้ว" ให้อัตโนมัติ
-- =============================================================================
alter table listings add column if not exists stock int;
